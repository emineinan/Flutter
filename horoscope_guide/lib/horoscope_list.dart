import 'package:flutter/material.dart';

class HoroscopeWidget extends StatelessWidget {
  const HoroscopeWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
